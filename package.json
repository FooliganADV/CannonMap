{
  "name": "cannonmap",
  "private": true,
  "version": "0.7.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
