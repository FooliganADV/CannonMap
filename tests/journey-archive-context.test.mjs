import test from 'node:test';import assert from 'node:assert/strict';
import {discoverJourneyProjects,journeyProjectManifest} from '../src/application/journey-archive-context.js';

test('active Project is included when lifecycle registration is absent',()=>{const projects=discoverJourneyProjects([],{projectId:'america-250',name:'America 250 – 2026',features:[{day:1}]});assert.equal(projects.length,1);assert.equal(projects[0].projectId,'america-250');});
test('active, completed, archived, and multiple Projects remain uniquely discoverable',()=>{const registered=[{projectId:'active',name:'Old snapshot'},{projectId:'done',lifecycleStatus:'archived'}],projects=discoverJourneyProjects(registered,{projectId:'active',name:'Current execution'});assert.deepEqual(projects.map(item=>item.projectId),['active','done']);assert.equal(projects[0].name,'Current execution');});
test('journey manifest reports day and Original/Evidence counts without fabrication',()=>{const entry=journeyProjectManifest({projectId:'p',name:'Trip',features:[{day:1},{day:9}],rallyExecution:{days:{1:{status:'complete'},9:{status:'complete'}}}},[{role:'original'},{role:'original'},{role:'evidence'}],'2026-08-08T12:00:00Z');assert.deepEqual(entry,{projectId:'p',projectName:'Trip',filename:'Trip_Backup.cmapproject',dayCount:2,originalPhotoCount:2,evidencePhotoCount:1,completedState:true,lifecycleStatus:'active',archivedAt:null,archiveTimestamp:'2026-08-08T12:00:00Z'});});
